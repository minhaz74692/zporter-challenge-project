{
  "name": "@zporter/shared",
  "version": "0.0.0",
  "private": true,
  "description": "Shared TypeScript types (challenge / participant / result / user DTOs) and the OpenAPI-generated API client, consumed by @zporter/api and @zporter/web.",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "echo \"no tests yet\" && exit 0",
    "lint": "echo \"no lint yet\" && exit 0"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
